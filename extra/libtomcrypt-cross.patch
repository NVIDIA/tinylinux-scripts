Fix cross-compilation

--- a/makefile.shared
+++ b/makefile.shared
@@ -16,6 +16,7 @@
 
 PLATFORM := $(shell uname | sed -e 's/_.*//')
 
+LIBTOOL=/tmp/crosslibtool
 ifeq ($(LIBTOOL),rlibtool)
   TGTLIBTOOL:=slibtool-shared
 else
